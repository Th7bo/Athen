{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "A very awesome Hypixel SkyBlock mod.",
  "authors": [
    "Starred"
  ],
  "contact": {},
  "license": "BSD 3-Clause",
  "icon": "assets/athen/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      {
        "value": "foo.starred.athen.Athen",
        "adapter": "kotlin"
      }
    ],
    "modmenu": [
      {
        "value": "foo.starred.athen.config.modMenu.Impl",
        "adapter": "kotlin"
      }
    ]
  },
  "mixins": [
    "${id}.mixins.json"
  ],
  "accessWidener": "${accessWidener}",
  "depends": {
    "fabricloader": ">=0.18",
    "fabric-api": "*",
    "fabric-language-kotlin": ">=${kotlin}",
    "hypixel-mod-api": ">=${hm_api}",
    "skyblock-api": ">=${sb_api}",
    "minecraft": "${minecraft}"
  },
  "custom": {
    "modmenu": {
      "update_checker": false
    }
  }
}